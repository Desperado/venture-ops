# Scan Mode

Use `npm run scan` to match `investors.yml` against `config/profile.yml` and append new targets to `data/pipeline.md`.

After scanning:
- summarize how many targets were found
- name the strongest matches
- recommend which pending targets to evaluate first

For live web discovery beyond configured targets, browse and add candidates to `investors.yml` only after the user confirms the universe to track.
